<%= form_for(@side_type) do |f| %>
  <% if @side_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@side_type.errors.count, "error") %> prohibited this side_type from being saved:</h2>

      <ul>
      <% @side_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
