<%= form_for(@meal_side_type) do |f| %>
  <% if @meal_side_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_side_type.errors.count, "error") %> prohibited this meal_side_type from being saved:</h2>

      <ul>
      <% @meal_side_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
